# Copy this file to `.env` and fill in values.
# Frontend (Vite) configuration
VITE_API_URL=/api
VITE_API_BASE_URL=/api
# Optional: where `npm run dev` proxies /api (default http://127.0.0.1:5001 — same as Flask app.py when run locally)
# DASHBOARD_DEV_API_PROXY=http://127.0.0.1:5000
